name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls -l
      - run: cd ./__tests__/playwright
      - run: docker compose up
